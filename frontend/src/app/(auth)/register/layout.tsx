export const metadata = {
  title: "Create a Riot Account",
};

export default function RegisterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <>
      <div className="">Sign In Layout </div>
      {children}
    </>
  );
}
